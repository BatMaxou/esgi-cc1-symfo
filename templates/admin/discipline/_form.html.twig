{{ form_start(form, {
    'attr': {'class': 'form generated'}
}) }}
    {{ form_widget(form) }}
    <button class="btn success">{{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }}
